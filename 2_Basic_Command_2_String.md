# String

combine:
```
string1+string2
```

split:

b.split(marker)   --- split a value to a list by a marker.
```
b="xxx, yyy" 
b.split(",")  --- ["xxx", "yyy"]
p,q = b.split(",")   --- p="xxx" , q="yyy"
```
